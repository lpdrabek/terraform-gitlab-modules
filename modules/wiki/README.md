# Warning

Since creation and destruction of the wiki pages are half-assed, this module is half-assed as well.

## How half-assed?

The pages create, but each page means new run of `apply` - the API seems to only create and destroy them one by one